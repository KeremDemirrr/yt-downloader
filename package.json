{
  "name": "yt-downloader",
  "version": "1.0.0",
  "description": "YouTube Video & MP3 Downloader — Render deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "pip install yt-dlp --user || pip3 install yt-dlp || echo 'yt-dlp install fallback'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
