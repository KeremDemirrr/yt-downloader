{
  "name": "yt-downloader",
  "version": "1.0.0",
  "description": "YouTube Video & MP3 Downloader — Render deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "pip3 install yt-dlp || pip install yt-dlp || echo 'pip not available, yt-dlp must be pre-installed'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
